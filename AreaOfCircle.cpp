#include <iostream>
using namespace std;
int main()
{
    float r, pai = 3.14159, ans;
    cout << "Enter the radious(r) = ";
    cin >> r;

    ans = pai * (r * r);
    cout << "Area of circle is = " << ans;
}
